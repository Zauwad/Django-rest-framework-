from django.db import models                    # Imports models module containing Django DB field types

# for adding databases models are used. We can define our data structure using models, and Django will handle the database interactions for us. By creating models, we can easily perform CRUD (Create, Read, Update, Delete) operations on our data without having to write raw SQL queries. Models also allow us to define relationships between different data entities, making it easier to manage complex data structures in our application.


# Create your models here.

