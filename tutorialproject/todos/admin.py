from django.contrib import admin                    # Imports the Django admin site module for registry and management

#for registering the models in the admin site, so that we can manage our data through the Django admin interface. By registering our models here, we can easily add, edit, and delete records in the database using the admin site without having to write custom views or forms.
# Register your models here.
